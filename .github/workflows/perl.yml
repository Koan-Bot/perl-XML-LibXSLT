name: perl

on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:

jobs:

  perl-versions:
    runs-on: ubuntu-latest
    name: List Perl versions
    outputs:
      perl-versions: ${{ steps.action.outputs.perl-versions }}
    steps:
      - id: action
        uses: perl-actions/perl-versions@v2
        with:
          since-perl: 5.14
          with-devel: true

  linux:
    name: "Perl ${{ matrix.perl-version }}"
    needs: [perl-versions]
    runs-on: ubuntu-latest

    env:
      PERL_USE_UNSAFE_INC: 0
      AUTOMATED_TESTING: 1
      PERL_CPANM_OPT: ""
      PERL5LIB: ""
      PERL5OPT: ""
      PERL_LOCAL_LIB_ROOT: ""
      PERL_MB_OPT: ""
      PERL_MM_OPT: ""

    strategy:
      fail-fast: false
      matrix:
        perl-version: ${{ fromJson(needs.perl-versions.outputs.perl-versions) }}

    container:
      image: perldocker/perl-tester:${{ matrix.perl-version }}

    steps:
      - uses: actions/checkout@v6
      - name: perl -V
        run: perl -V
      - name: Install native dependencies
        run: >
          apt-get update &&
          apt-get install -y --no-install-recommends
          libxml2-dev libxslt1-dev pkg-config
      - name: Install CPAN dependencies
        run: cpanm --notest --installdeps .
      - name: Makefile.PL
        run: perl Makefile.PL
      - name: make test
        run: make test

  macos:
    name: macOS
    runs-on: macos-latest

    env:
      PERL_USE_UNSAFE_INC: 0
      AUTOMATED_TESTING: 1
      PKG_CONFIG_PATH: /opt/homebrew/opt/libxslt/lib/pkgconfig:/opt/homebrew/opt/libxml2/lib/pkgconfig:/opt/homebrew/opt/icu4c/lib/pkgconfig

    steps:
      - uses: actions/checkout@v6
      - name: Install native dependencies
        run: brew install libxslt libxml2 pkg-config cpanminus
      - name: perl -V
        run: perl -V
      - name: Install CPAN dependencies
        run: cpanm --notest --installdeps .
      - name: Makefile.PL
        run: perl Makefile.PL
      - name: make test
        run: make test
